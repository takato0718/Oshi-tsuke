<div class="col-12">
  <div class="card shadow-sm <%= 'border-secondary' if recommendation.skipped? %>">
    <div class="card-body">
      <%= render 'users/recommendation_header', recommendation: recommendation %>
      <%= render 'users/recommendation_footer', recommendation: recommendation %>
    </div>
  </div>
</div>
