<!DOCTYPE html>
<html lang="ja">
  <head>
    <title>推しツケ - あなたの推しを押し付ける</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

        <!-- OGP設定 -->
    <meta property="og:title" content="<%= content_for?(:og_title) ? yield(:og_title) : '推しツケ - あなたの推しを押し付ける' %>">
    <meta property="og:description" content="<%= content_for?(:og_description) ? yield(:og_description) : '推しツケは、あなたの推しを世界に押し付けるサービスです' %>">
    <meta property="og:image" content="<%= content_for?(:og_image) ? yield(:og_image) : asset_url('ogp.png') %>">
    <meta property="og:url" content="<%= request.original_url %>">
    <meta property="og:type" content="website">
    <meta property="og:site_name" content="推しツケ">

    <!-- Twitter Card設定 -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= content_for?(:og_title) ? yield(:og_title) : '推しツケ - あなたの推しを押し付ける' %>">
    <meta name="twitter:description" content="<%= content_for?(:og_description) ? yield(:og_description) : '推しツケは、あなたの推しを世界に押し付けるサービスです' %>">
    <meta name="twitter:image" content="<%= content_for?(:og_image) ? yield(:og_image) : asset_url('ogp.png') %>">
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application", type: "module", "data-turbo-track": "reload" %>
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="d-flex flex-column min-vh-100">

  <!-- 汎用ローディングオーバーレイ -->
    <div id="loading-overlay" class="loading-overlay" style="display: none;">
      <div class="water-ripple">
        <div class="ripple ripple-1"></div>
        <div class="ripple ripple-2"></div>
        <div class="ripple ripple-3"></div>
      </div>
    </div>
    
  <!-- ナビゲーションバー -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container">
        <%= link_to "推しツケ", root_path, class: "navbar-brand fw-bold" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <%= link_to "投稿一覧", posts_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "今日の推し", daily_recommendations_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "コミュニティ", communities_path, class: "nav-link" %>
            </li>
          </ul>
          <ul class="navbar-nav">
            <% if logged_in? %>
              <li class="nav-item">
                <%= link_to profile_path, class: "nav-link" do %>
                  <i class="bi bi-person-circle"></i> <%= current_user.name %>
                <% end %>
              </li>
              <li class="nav-item">
                <%= button_to logout_path, method: :delete, class: "nav-link" do %>
                  ログアウト
                <% end %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "ログイン", new_session_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "新規登録", new_registration_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- メインコンテンツ -->
    <main class="container my-4 flex-grow-1">
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>
      <% if alert %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <!-- フッター -->
  <footer class="bg-light py-4 mt-auto">
    <div class="container">
      <div class="d-flex flex-column flex-md-row justify-content-between align-items-center">
        <p class="text-muted mb-2 mb-md-0">&copy; 2024 推しツケ. All rights reserved.</p>
        <!-- お問い合わせリンク -->
        <div class="footer-links d-flex flex-wrap align-items-center gap-3">

        <%= link_to '利用規約', terms_path, class: 'footer-link text-muted text-decoration-none' %>
        
        <%= link_to 'プライバシーポリシー', privacy_path, class: 'footer-link text-muted text-decoration-none' %>

        <%= link_to 'https://docs.google.com/forms/d/e/1FAIpQLSejhjOOUSNoqZRfBnnvofv0s4feajatt5TfGTHDD25To7snig/viewform?usp=header', target: '_blank', rel: 'noopener noreferrer', class: 'footer-link text-muted' do %>
        お問い合わせ
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-envelope ms-1" viewBox="0 0 16 16" style="display: inline-block; vertical-align: text-bottom;">
          <path d="M0 4a2 2 0 0 1 2-2h12a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2H2a2 2 0 0 1-2-2V4Zm2-1a1 1 0 0 0-1 1v.217l7 4.2 7-4.2V4a1 1 0 0 0-1-1H2Zm13 2.383-4.708 2.825L15 11.105V5.383Zm-.034 6.876-5.64-3.471L8 9.583l-1.326-.795-5.64 3.47A1 1 0 0 0 2 13h12a1 1 0 0 0 .966-.741ZM1 11.105l4.708-2.897L1 5.383v5.722Z"/>
        </svg>
        <% end %>
        </div>
      </div>
    </div>
  </footer>
  
  </body>
</html>
