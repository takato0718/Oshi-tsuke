<!DOCTYPE html>
<html lang="ja">
  <head>
    <title>推しツケ - あなたの推しを押し付ける</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <%= stylesheet_link_tag "application" %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container">
        <%= link_to "推しツケ", root_path, class: "navbar-brand fw-bold" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <p>"投稿一覧"</p>
            </li>
            <li class="nav-item">
              <p>"今日の推し"</p>
            </li>
          </ul>
          <ul class="navbar-nav">
            <% if logged_in? %>
              <li class="nav-item">
                <%= link_to root_path, class: "nav-link" do %> <!--仮にroot_pathとする実際はuser_path-->
                  <i class="bi bi-person-circle"></i> <%= current_user.name %>
                <% end %>
              </li>
              <li class="nav-item">
                <%= button_to logout_path, method: :delete, class: "btn btn-link nav-link p-0" do %>
                  ログアウト
                <% end %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "ログイン", new_session_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "新規登録", new_registration_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- メインコンテンツ -->
    <main class="container my-4">

      <%= yield %>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
