{
  "name": "oshi-tsuke",
  "private": true,
  "dependencies": {
    "@hotwired/stimulus": "^3.2.0",
    "@hotwired/turbo-rails": "^7.3.0",
    "@popperjs/core": "^2.11.8",
    "bootstrap": "5.3.8",
    "esbuild": "^0.17.6"
  },
  "scripts": {
    "build": "esbuild app/javascript/application.js --bundle --format=esm --outdir=app/assets/builds --public-path=/assets",
    "build:css": "echo \"CSS build completed\""
  }
}
